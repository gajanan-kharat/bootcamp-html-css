{
  "name": "bootcamp-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev",
    "docker:build": "docker build -t bootcamp-app -f backend/Dockerfile .",
    "docker:up": "docker-compose up --build"
  }
}
